# เอกสารความต้องการระบบ — v0.4 (เปลี่ยนทิศ: เครื่องมือวิเคราะห์สินค้า)
## ระบบช่วยวิเคราะห์สินค้า + จัดการลิงก์ Affiliate (Zero-Cost, Facebook-first)

> เวอร์ชัน 0.4 — ปรับจาก v0.3 ตามทิศใหม่: เลิกใช้ Affiliate Network/third-party
> หลักการเดิม: ไม่มีค่า API + รันบนเครื่องตัวเอง

---

## 0. สิ่งที่เปลี่ยนจาก v0.3 (สำคัญ)

- ❌ เลิกใช้ Affiliate Network (Involve Asia/AccessTrade) และการสร้างลิงก์ผ่าน third-party
- ✅ ได้ข้อมูลสินค้าจาก **ส่วนเสริมเบราว์เซอร์** ที่อ่านหน้าสินค้าที่ผู้ใช้เปิดดูเอง
- ✅ ลิงก์ affiliate มาจาก **โปรแกรม affiliate ของแพลตฟอร์มเอง** (ผู้ใช้สร้างลิงก์เองแล้ววางเก็บในระบบ)
- ✅ เพิ่มแกน **"วิเคราะห์ + ให้คะแนนความคุ้มค่าในการโปรโมต"** เป็นหัวใจใหม่
- ⚠️ การติดตามรายได้จริงเปลี่ยนเป็น **กรอกเอง/ดูใน dashboard ของแพลตฟอร์ม** (ไม่มี API อัตโนมัติแล้ว)

---

## 1. การตัดสินใจที่ล็อกแล้ว (Locked Decisions)

| หัวข้อ | สรุป |
|---|---|
| เป้าหมายหลัก | วิเคราะห์สินค้าจาก Shopee/Lazada/TikTok เพื่อเลือกตัวที่น่าโปรโมต + จัดการลิงก์ให้ง่าย |
| วิธีป้อนสินค้า | **ส่วนเสริมเบราว์เซอร์** (หลัก) + วาง URL (fallback แบบ lite) |
| แหล่งข้อมูลสินค้า | อ่านจากหน้าสินค้าที่ผู้ใช้เปิดดูเอง (ไม่ใช้ network/ไม่ mass-scrape) |
| ลิงก์ affiliate | โปรแกรม affiliate ของแพลตฟอร์มเอง — ผู้ใช้สร้าง/วางลิงก์เอง |
| สิ่งที่วิเคราะห์ | ราคา/ส่วนลด, รีวิว/เรตติ้ง, ยอดขาย/ความนิยม, %คอมมิชชัน (กรอกเอง) → รวมเป็นคะแนน |
| เผยแพร่ | Facebook Page (X ไว้เฟสหลัง) |
| ร่างแคปชัน | Ollama (CPU) + Qwen 2.5 + เทมเพลต fallback, ไทย+อังกฤษ |
| ติดตามรายได้ | กรอกเอง/ดู dashboard แพลตฟอร์ม |
| โฮสต์ | เครื่องตัวเอง (RAM 32GB / Ryzen 5 1600X) |

---

## 2. ภาพรวม (Overview)

**Vision:** เครื่องมือ "ก่อนโพสต์" ที่ช่วยตัดสินใจว่าสินค้าไหนน่าโปรโมต — ขณะเปิดดูสินค้าบน Shopee/Lazada/TikTok กดปุ่มจาก extension เพื่อเก็บข้อมูล → ระบบให้คะแนนความคุ้มค่า → แนบลิงก์ affiliate ที่สร้างเอง → ร่างแคปชัน → โพสต์ลง FB Page → บันทึกผลด้วยตัวเอง โดยไม่มีค่า API

**ลูปการใช้งาน:**
เปิดหน้าสินค้า → กด extension เก็บข้อมูล → กรอก %คอมมิชชัน + วางลิงก์ affiliate → ระบบคำนวณคะแนน → เปรียบเทียบ/เลือกสินค้าในแดชบอร์ด → ร่างแคปชัน → อนุมัติ → โพสต์ FB → (ภายหลัง) กรอกยอดที่ได้จริง

---

## 3. ขอบเขต (Scope)

**In Scope**
- ส่วนเสริมเบราว์เซอร์เก็บข้อมูลสินค้าจากหน้าที่เปิดดู
- เก็บ/normalize ข้อมูล + คำนวณคะแนนความคุ้มค่าในการโปรโมต
- จัดการลิงก์ affiliate ที่ผู้ใช้สร้างเอง (เก็บคู่กับสินค้า)
- เปรียบเทียบ/จัดอันดับสินค้าในแดชบอร์ด
- ร่างแคปชัน (Ollama/เทมเพลต) → อนุมัติ → โพสต์ FB Page
- บันทึกผลรายได้แบบกรอกเอง

**Out of Scope**
- ใช้ Affiliate Network / third-party API
- ดึงข้อมูล/สร้างลิงก์อัตโนมัติผ่าน API ของแพลตฟอร์ม (ไม่มีให้ฟรี)
- mass scraping ทั้งเว็บ / ดึงสินค้าจำนวนมากอัตโนมัติ
- โพสต์/อ่านข้อมูล X ผ่าน API (เฟสหลัง = โหมดร่างให้ก็อป)
- ดึง conversion อัตโนมัติ (ไม่มี API)

---

## 4. ผู้ใช้งาน (Actors)

| Actor | คำอธิบาย |
|---|---|
| **Operator/เจ้าของ** | เปิดดูสินค้า, กดเก็บข้อมูล, กรอกคอมมิชชัน/ลิงก์, เลือกสินค้า, อนุมัติ, ดูแดชบอร์ด |
| **Browser Extension** | อ่านข้อมูลจากหน้าสินค้าปัจจุบัน แล้วส่งเข้า backend (localhost) |
| **Local Backend (Worker)** | คำนวณคะแนน, ร่างแคปชัน (Ollama), โพสต์ FB ตามเวลา |
| **External (ฟรี)** | Facebook Graph API (เพจตัวเอง) |

---

## 5. ข้อจำกัดสำคัญ (Constraints)

- **C-1** งบ API = 0 (ทุกอย่างฟรี/รันเอง)
- **C-2** ข้อมูลสินค้าอ่านจาก **หน้าที่ผู้ใช้เปิดดูเองเท่านั้น** ที่ความเร็วการใช้งานปกติ — ไม่ mass-harvest (ลดความเสี่ยง ToS/โดนบล็อก)
- **C-3** โครงสร้าง HTML ของแพลตฟอร์มเปลี่ยนได้ → ตัว parser ใน extension ต้องออกแบบให้แก้ selector ง่าย และมีจุด fallback
- **C-4** %คอมมิชชัน + ลิงก์ affiliate = ผู้ใช้กรอก/วางเอง (ไม่มีบนหน้าสินค้า)
- **C-5** ไม่มี API วัด conversion → รายได้จริงกรอกเอง
- **C-6** Ollama รันบน CPU (RX 480 ไม่รองรับ ROCm) → โมเดลเล็ก, งานร่างเป็น batch; มีเทมเพลต fallback
- **C-7** Facebook ฟรีเฉพาะเพจของเราเอง

---

## 6. ความต้องการเชิงฟังก์ชัน (Functional Requirements)

### M1 — เก็บข้อมูลสินค้า (Browser Extension)
- **FR-M1-1** ปุ่มใน extension เก็บข้อมูลจากหน้าสินค้า Shopee/Lazada/TikTok ที่เปิดอยู่: ชื่อ, ราคา, ส่วนลด, เรตติ้ง, จำนวนรีวิว, ยอดขาย/จำนวนที่ขายได้ (เท่าที่หน้านั้นแสดง), ร้านค้า, URL สินค้า
- **FR-M1-2** ส่งข้อมูลเข้า backend ที่ localhost
- **FR-M1-3** ถ้าฟิลด์ไหนอ่านไม่ได้ → ปล่อยว่างให้กรอกเองภายหลัง ไม่ทำให้พังทั้งชุด
- **FR-M1-4** (fallback) รับการ "วาง URL" เพื่อบันทึกสินค้าแบบข้อมูลพื้นฐาน

### M2 — วิเคราะห์ & ให้คะแนน (Analyze & Score)
- **FR-M2-1** normalize ข้อมูลเป็นฟิลด์มาตรฐาน (ราคา, %ส่วนลด, เรตติ้ง, #รีวิว, ยอดขาย, %คอมมิชชัน)
- **FR-M2-2** ผู้ใช้กรอก %คอมมิชชันต่อสินค้า/ร้าน
- **FR-M2-3** คำนวณ **คะแนนความคุ้มค่าในการโปรโมต** = ฟังก์ชันถ่วงน้ำหนักของ (%คอมมิชชัน, ยอดขาย/ความนิยม, เรตติ้ง, #รีวิว, ส่วนลด) — น้ำหนักปรับได้ในตั้งค่า
- **FR-M2-4** จัดอันดับ/กรอง/เปรียบเทียบสินค้าในตาราง

### M3 — จัดการลิงก์ Affiliate (Manual)
- **FR-M3-1** ผู้ใช้วางลิงก์ affiliate (ที่สร้างจากโปรแกรมของแพลตฟอร์มเอง) เก็บคู่กับสินค้า
- **FR-M3-2** ตรวจรูปแบบลิงก์เบื้องต้น + ปุ่มคัดลอกลิงก์ใช้งานง่าย
- **FR-M3-3** (ตัวเลือก) ใส่แท็ก/หมายเหตุระบุแคมเปญ เพื่อช่วยจำตอนไปดูยอดใน dashboard แพลตฟอร์ม

### M4 — ร่างแคปชัน (Ollama/เทมเพลต)
- **FR-M4-1** ร่างแคปชันไทย/อังกฤษจากข้อมูลสินค้า + โทน โดยเลือก Ollama หรือเทมเพลต
- **FR-M4-2** แนบลิงก์ affiliate + ข้อความเปิดเผย (#ad) อัตโนมัติ
- **FR-M4-3** Ollama timeout → fallback เทมเพลต + แจ้งผู้ใช้
- **FR-M4-4** แก้ไขก่อนบันทึกได้

### M5 — คิวอนุมัติ
- **FR-M5-1** ร่างเข้าสถานะรออนุมัติ; อนุมัติ/แก้ไข/ปฏิเสธรายโพสต์
- **FR-M5-2** เฉพาะที่อนุมัติแล้วจึงโพสต์ได้

### M6 — โพสต์ Facebook Page
- **FR-M6-1** โพสต์ทันที/ตั้งเวลา ผ่าน Graph API
- **FR-M6-2** สำเร็จ → เก็บ fbPostId; ล้มเหลว → retry + log
- **FR-M6-3** Rate guard: จำกัดโพสต์/วัน

### M7 — บันทึกผล (Manual Tracking)
- **FR-M7-1** ผู้ใช้กรอกยอด click/order/commission ที่ดูจาก dashboard ของแพลตฟอร์มได้ (ต่อสินค้า/แคมเปญ)
- **FR-M7-2** เก็บประวัติเพื่อดูแนวโน้มและเทียบกับ "คะแนนความคุ้มค่า" ที่ทำนายไว้ (เช็คว่าโมเดลคะแนนแม่นไหม)

### M8 — แดชบอร์ด
- **FR-M8-1** ตารางสินค้าพร้อมคะแนน + จัดอันดับ/กรอง (หัวใจของเครื่องมือ)
- **FR-M8-2** หน้าเปรียบเทียบสินค้าแบบเทียบกันหลายตัว
- **FR-M8-3** สถานะร่าง/คิวอนุมัติ/โพสต์
- **FR-M8-4** สรุปผลที่กรอกเอง (รายได้/แนวโน้ม)

### M9 — ตั้งค่า
- **FR-M9-1** เชื่อม Facebook Page (OAuth) + เก็บ token เข้ารหัส
- **FR-M9-2** ตั้งน้ำหนักการให้คะแนน, โทนเนื้อหา, ลิมิตโพสต์/วัน, โหมดร่าง (Ollama/เทมเพลต)

---

## 7. ความต้องการที่ไม่ใช่ฟังก์ชัน (NFR)

- **NFR-1 Zero-cost:** ฟรี/โอเพนซอร์ส/รันเองทั้งหมด
- **NFR-2 Compliance:** อ่านเฉพาะหน้าที่ผู้ใช้เปิดเอง, โพสต์เฉพาะเพจตัวเอง, ใส่ disclosure
- **NFR-3 Resilient parser:** selector ของ extension แยกเป็น config แก้ง่ายเมื่อเว็บเปลี่ยน
- **NFR-4 Security:** token/secret เข้ารหัส, extension ↔ backend สื่อสารผ่าน localhost เท่านั้น
- **NFR-5 Fallback:** Ollama ล่ม → เทมเพลต; ฟิลด์อ่านไม่ได้ → กรอกเอง
- **NFR-6 รันบนเครื่องเดียวได้**

---

## 8. โครงสร้างข้อมูล (Core Entities)

- **Product** (id, platform, name, price, discountPct, rating, reviewCount, salesCount, shop, productUrl, capturedAt)
- **ProductExtra** (productId, commissionPct *(กรอกเอง)*, affiliateUrl *(วางเอง)*, campaignTag, score)
- **PostDraft** (id, productId, captionTH, captionEN, status, scheduledAt)
- **PublishedPost** (id, draftId, fbPostId, publishedAt)
- **ResultEntry** (id, productId/campaignTag, clicks, orders, commission, enteredAt) *(กรอกเอง)*
- **Page** (fbPageId, accessTokenEnc)
- **Setting** (scoreWeights, draftMode, dailyPostLimit, defaultTone)

---

## 9. สถาปัตยกรรม & Tech Stack (ฟรีทั้งหมด)

- **Browser Extension:** Chrome/Firefox (Manifest V3) — content script อ่าน DOM หน้าสินค้า + ปุ่มเก็บข้อมูล; ส่งเข้า backend ผ่าน `http://localhost`
- **Backend/API:** TypeScript + Elysia (Bun)
- **DB:** SQLite (ไฟล์เดียว)
- **Scheduler:** in-process cron (สำหรับโพสต์ตั้งเวลา FB)
- **AI:** Ollama (local) + Qwen 2.5 + เทมเพลต fallback
- **Frontend แดชบอร์ด:** Next.js/React + Tailwind + Recharts
- **External (ฟรี):** Facebook Graph API (Page)

**หมายเหตุ:** ส่วน parser ต่อแพลตฟอร์ม (Shopee/Lazada/TikTok) แยกเป็นโมดูลละไฟล์ + เก็บ selector ใน config เพื่อแก้ง่ายเมื่อเว็บเปลี่ยนหน้าตา

---

## 10. MVP เฟส 1 — Backlog (พร้อมพัฒนา)

> เป้าหมาย MVP: เปิดหน้าสินค้า → เก็บข้อมูล → ให้คะแนน → เห็นตารางจัดอันดับ → แนบลิงก์ → ร่าง+โพสต์ FB

**US-1 ส่วนเสริมเก็บข้อมูล**
- เปิดหน้าสินค้า Shopee แล้วกดปุ่ม extension → ดึงชื่อ/ราคา/เรตติ้ง/รีวิว/ยอดขาย/URL ส่งเข้า backend สำเร็จ
- ฟิลด์ที่อ่านไม่ได้แสดงเป็นค่าว่างให้กรอกเอง
- (เริ่ม Shopee ก่อน 1 แพลตฟอร์ม แล้วค่อยเพิ่ม Lazada/TikTok)

**US-2 กรอกคอมมิชชัน + ลิงก์ + ให้คะแนน**
- กรอก %คอมมิชชัน และวางลิงก์ affiliate ต่อสินค้าได้
- ระบบคำนวณคะแนนความคุ้มค่าตามน้ำหนักที่ตั้งไว้
- ปรับน้ำหนักแล้วคะแนนอัปเดต

**US-3 แดชบอร์ดเปรียบเทียบ/จัดอันดับ**
- ตารางสินค้าเรียงตามคะแนน, กรองตามแพลตฟอร์ม/ช่วงราคา/เรตติ้ง
- เลือกหลายตัวมาเทียบกันได้

**US-4 ร่างแคปชัน (เทมเพลตก่อน → Ollama)**
- ร่างไทย/อังกฤษ แนบลิงก์ + #ad อัตโนมัติ; แก้ไขได้
- Ollama ล่ม → fallback เทมเพลต

**US-5 อนุมัติ + โพสต์ FB**
- อนุมัติร่าง → โพสต์/ตั้งเวลา FB Page; เก็บ fbPostId; rate guard ทำงาน

**US-6 บันทึกผลเอง**
- กรอกยอด click/order/commission ที่ดูจาก dashboard แพลตฟอร์ม และดูย้อนหลังได้

---

## 11. ลำดับการพัฒนาแนะนำ

1. โครง backend + DB schema + ตั้งค่าเชื่อม FB
2. **US-1** extension (เริ่ม Shopee) + endpoint รับข้อมูล
3. **US-2** คอมมิชชัน/ลิงก์ + เครื่องคิดคะแนน
4. **US-3** แดชบอร์ดตาราง/เปรียบเทียบ (ส่งมอบคุณค่าหลักได้แล้ว)
5. **US-4** ร่างเทมเพลต → เพิ่ม Ollama
6. **US-5** อนุมัติ + โพสต์ FB
7. **US-6** บันทึกผลเอง
8. ขยาย parser ไป Lazada/TikTok

---

## 12. ความเสี่ยง & คำถามที่ยังเปิดอยู่

**ความเสี่ยง**
- R-1 เว็บแพลตฟอร์มเปลี่ยนโครงสร้าง → parser พัง → บรรเทาด้วย selector แบบ config + กรอกมือสำรอง
- R-2 บางแพลตฟอร์มจำกัด/ตรวจจับ extension → ทดสอบทีละเจ้า, อ่านที่ความเร็วปกติ
- R-3 คะแนนความคุ้มค่าอาจไม่ตรงจริง → ใช้ US-6 เทียบผลจริงเพื่อปรับน้ำหนัก

**คำถามที่ต้องการคำตอบ**
1. เริ่มทำ extension บนเบราว์เซอร์อะไรก่อน (Chrome หรือ Firefox)?
2. ให้เริ่ม parser ที่แพลตฟอร์มไหนก่อน (แนะนำเลือก 1 ตัวที่คุณใช้บ่อยสุด)?
3. อยากให้ "คะแนนความคุ้มค่า" เน้นน้ำหนักด้านไหนมากสุด (คอมมิชชัน vs ยอดขาย vs เรตติ้ง)?

---
*หมายเหตุ: การอ่านข้อมูลจากหน้าเว็บควรทำในขอบเขตการใช้งานส่วนตัวบนหน้าที่คุณเปิดดูเอง และควรตรวจ ToS ของแต่ละแพลตฟอร์มก่อนใช้งานจริง*
