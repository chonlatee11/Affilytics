---
phase: 02-product-capture-shopee
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - backend/src/db/schema.ts
  - backend/drizzle/0001_add_product_url_unique.sql
  - backend/src/routes/products.ts
  - backend/index.ts
autonomous: true
requirements: [CAP-02, CAP-03, CAP-04]
must_haves:
  truths:
    - "POST /api/products/capture persists a captured product into the products table"
    - "Re-capturing the same productUrl updates the existing row instead of duplicating it"
    - "Null/missing optional fields are stored as null without failing the capture (CAP-03)"
    - "A minimal payload (platform + name + price + URL) creates a basic record (CAP-04 backend support)"
  artifacts:
    - path: "backend/src/routes/products.ts"
      provides: "POST /api/products/capture — TypeBox-validated upsert on product_url"
      exports: ["productsRoutes"]
      min_lines: 30
    - path: "backend/drizzle/0001_add_product_url_unique.sql"
      provides: "UNIQUE index on products.product_url enabling onConflictDoUpdate"
      contains: "UNIQUE INDEX"
    - path: "backend/src/db/schema.ts"
      provides: "products.productUrl marked .unique()"
      contains: ".notNull().unique()"
  key_links:
    - from: "backend/src/routes/products.ts"
      to: "products.productUrl unique index"
      via: "onConflictDoUpdate target"
      pattern: "onConflictDoUpdate"
    - from: "backend/index.ts"
      to: "productsRoutes"
      via: "dynamic import + .use()"
      pattern: "productsRoutes"
---

<objective>
Deliver the backend half of the capture round-trip: add a UNIQUE constraint on `products.product_url` (via a tracked Drizzle migration), implement `POST /api/products/capture` with TypeBox validation and a `onConflictDoUpdate` upsert, and mount it in `index.ts`. After this plan, the GREEN backend tests prove a product can be captured, re-captured (upsert), and persisted with graceful null fields.

Purpose: This is the persistence endpoint the extension will call. It is the first entity-write endpoint and the integration target for CAP-02/03/04. The upsert (D-05) keeps the dashboard clean on re-capture.
Output: Migration 0001, productsRoutes, index.ts wiring — backend capture tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-product-capture-shopee/02-RESEARCH.md
@.planning/phases/02-product-capture-shopee/02-PATTERNS.md

@backend/src/db/schema.ts
@backend/index.ts
@backend/src/routes/settings.ts

<interfaces>
<!-- Existing products table (backend/src/db/schema.ts lines 13-26). productUrl currently NOT unique. -->
products: { id (uuid pk), platform enum['shopee','lazada','tiktok'] notNull, name notNull,
price real notNull, discountPct real notNull default 0, rating real (nullable),
reviewCount integer (nullable), salesCount integer (nullable), shop text (nullable),
productUrl text('product_url') notNull, capturedAt integer timestamp default unixepoch() }

<!-- Existing UNIQUE pattern in same file (line 85): fbPageId: text('fb_page_id').notNull().unique() -->
<!-- Route mount pattern (index.ts lines 35-37, 72-74): dynamic await import() then app.use() at TOP LEVEL (not inside import.meta.main). -->
<!-- Migration scripts (backend/package.json): migrate:generate = drizzle-kit generate; migrate:run = bun run src/db/migrate.ts (importing client applies migrations). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add UNIQUE(product_url) to schema and generate+run the migration [BLOCKING]</name>
  <files>backend/src/db/schema.ts, backend/drizzle/0001_add_product_url_unique.sql</files>
  <read_first>
    - backend/src/db/schema.ts (products table lines 13-26; existing .unique() at line 85)
    - .planning/phases/02-product-capture-shopee/02-PATTERNS.md (schema.ts modify section lines 133-163; migration section lines 166-182)
    - .planning/phases/02-product-capture-shopee/02-RESEARCH.md (Pattern 7 lines 473-499; Pitfall 5 lines 630-634)
    - backend/src/db/migrate.ts (migrate:run applies migrations by importing client)
  </read_first>
  <behavior>
    - After the migration runs, the products table has a UNIQUE index on product_url
    - Inserting two rows with the same product_url raises a UNIQUE constraint error (proving the index exists), which the upsert in Task 2 will resolve via onConflictDoUpdate
  </behavior>
  <action>
    In `backend/src/db/schema.ts`, change the `productUrl` column (line 23) from `text('product_url').notNull()` to `text('product_url').notNull().unique()` — mirror the existing `fbPageId ... .notNull().unique()` chain. Then run `cd backend && bun run migrate:generate` (drizzle-kit generate) which creates a new migration SQL under `backend/drizzle/` containing a `CREATE UNIQUE INDEX ... ON products (product_url)` statement (SQLite has no ALTER TABLE ADD CONSTRAINT — the generated table-rebuild or CREATE UNIQUE INDEX is expected; do NOT hand-write the SQL). The generated filename will differ from `0001_add_product_url_unique.sql` — that is fine. This is [BLOCKING]: the migration MUST be generated AND applied (`cd backend && bun run migrate:run`) before Task 2's upsert can be verified, because `onConflictDoUpdate` requires the unique index to exist at runtime. NOTE: type checks pass without the migration (types come from schema.ts), so this step is easy to skip and produces a false-positive — it is mandatory.
  </action>
  <verify>
    <automated>cd backend && bun run migrate:generate && bun run migrate:run && ls drizzle/0001_*.sql 2>/dev/null | head -1 && grep -rl "UNIQUE INDEX" drizzle/ | grep -v 0000 && echo MIGRATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/db/schema.ts` productUrl line reads `text('product_url').notNull().unique()`
    - A new migration file exists under `backend/drizzle/` (not 0000) containing `UNIQUE INDEX` on `product_url`
    - `cd backend && bun run migrate:run` exits 0 (migration applied to the DB)
  </acceptance_criteria>
  <done>Schema marks product_url unique; tracked migration generated and applied; unique index exists in the live DB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement POST /api/products/capture (upsert) and mount it</name>
  <files>backend/src/routes/products.ts, backend/index.ts</files>
  <read_first>
    - backend/src/routes/settings.ts (analog: Elysia prefix route, TypeBox t.Object body, set.status error)
    - backend/index.ts (dynamic import + .use() at top level; import.meta.main guard)
    - .planning/phases/02-product-capture-shopee/02-PATTERNS.md (products.ts section lines 35-129; index.ts modify lines 185-222; upsert pattern lines 93-114)
    - .planning/phases/02-product-capture-shopee/02-RESEARCH.md (Pattern 8 lines 503-574)
    - backend/src/routes/products.test.ts (the RED tests this must satisfy — from plan 02-01)
  </read_first>
  <behavior>
    - POST /api/products/capture with a full valid body returns { ok:true, id } and writes one products row (CAP-02)
    - Posting the same productUrl twice leaves exactly one row, with name/price/rating/etc. updated to the latest values (D-05 upsert)
    - Body with null rating/reviewCount/salesCount/shop succeeds; nulls stored (CAP-03)
    - Minimal body (platform + name + price + productUrl) succeeds (CAP-04 backend support)
    - Invalid body (missing name, negative price, productUrl too short / bad format) is rejected by TypeBox before the handler runs
  </behavior>
  <action>
    Create `backend/src/routes/products.ts` exporting `productsRoutes = new Elysia({ prefix: '/api/products' })` with a `.post('/capture', handler, { body })`. Build the insert `row` from the body (default discountPct to 0; pass rating/reviewCount/salesCount/shop as null when absent; set `capturedAt: new Date()`). Upsert via `db.insert(products).values(row).onConflictDoUpdate({ target: products.productUrl, set: { name: sql\`excluded.name\`, price: sql\`excluded.price\`, discountPct: sql\`excluded.discount_pct\`, rating: sql\`excluded.rating\`, reviewCount: sql\`excluded.review_count\`, salesCount: sql\`excluded.sales_count\`, shop: sql\`excluded.shop\`, capturedAt: sql\`excluded.captured_at\` } }).returning({ id: products.id })`. Return `{ ok: true, id: result[0].id }`. TypeBox body: platform = `t.Union([t.Literal('shopee'),t.Literal('lazada'),t.Literal('tiktok')])`; name = `t.String({ minLength: 1 })`; price = `t.Number({ minimum: 0 })`; discountPct = `t.Optional(t.Number({ minimum:0, maximum:100 }))`; rating = `t.Optional(t.Nullable(t.Number({ minimum:0, maximum:5 })))`; reviewCount/salesCount = `t.Optional(t.Nullable(t.Integer({ minimum:0 })))`; shop = `t.Optional(t.Nullable(t.String({ maxLength: 500 })))`; name `maxLength: 500`; productUrl = `t.String({ minLength:10, maxLength: 2048, format:'uri' })`. Then in `backend/index.ts` add `const { productsRoutes } = await import('./src/routes/products')` alongside the other dynamic route imports (top level, after requireEncryptionKey()) and chain `.use(productsRoutes)` after `.use(fbOauthRoutes)`. Do NOT move the import inside the `import.meta.main` block.
  </action>
  <verify>
    <automated>cd backend && bun test src/routes/products.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/routes/products.ts` exports `productsRoutes` and contains `onConflictDoUpdate` targeting `products.productUrl`
    - `backend/index.ts` dynamically imports productsRoutes at top level and chains `.use(productsRoutes)`
    - `cd backend && bun test src/routes/products.test.ts` passes all CAP-02/03/04 cases (GREEN)
    - `cd backend && bun test` (full suite) stays green (no regression in settings/health/oauth)
  </acceptance_criteria>
  <done>Capture endpoint upserts on product_url, handles null fields and minimal payloads, is mounted, and backend tests are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Extension background SW → POST /api/products/capture | Untrusted JSON body crosses into the backend (the page-derived product data) |
| Backend → SQLite | Parameterized writes via Drizzle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-CAP-1 | Tampering | POST /capture body | mitigate | TypeBox `t.Object` validates every field: numeric bounds on price/discount/rating, integer bounds on counts, bounded string lengths (name/shop ≤500), productUrl format 'uri' + length bound |
| T-02-CAP-2 | Denial of Service | POST /capture oversized payload | mitigate | maxLength bounds on all strings (name 500, shop 500, productUrl 2048) reject oversized input at the Elysia layer |
| T-02-CAP-3 | Injection (SQL) | Drizzle upsert | mitigate | Drizzle parameterizes values; `sql\`excluded.*\`` references are column identifiers, not interpolated user input |
| T-02-CAP-4 | Elevation of Privilege | localhost endpoint reachable from other local apps | accept | Backend already binds 127.0.0.1 + CORS allowlist (Phase 1 invariant); capture route inherits it; single-operator L1 tool |
</threat_model>

<verification>
- `cd backend && bun run migrate:run` applies the UNIQUE index
- `cd backend && bun test` is fully green (capture + existing suites)
- product_url uniqueness enforced; re-capture updates not duplicates
</verification>

<success_criteria>
- CAP-02: capture persists; CAP-03: null fields stored without failure; CAP-04: minimal payload creates a record
- Upsert keyed on product_url (D-05); migration tracked and applied
</success_criteria>

<output>
Create `.planning/phases/02-product-capture-shopee/02-02-SUMMARY.md` when done
</output>
