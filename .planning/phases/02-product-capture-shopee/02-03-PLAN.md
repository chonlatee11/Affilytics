---
phase: 02-product-capture-shopee
plan: 03
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - extension/lib/normalizer.ts
  - extension/lib/messaging.ts
autonomous: true
requirements: [CAP-01]
must_haves:
  truths:
    - "Raw DOM text normalizes into schema-compatible numbers (price-range→lowest, k/Thai abbreviations→integer, currency stripped) — D-09"
    - "A typed messaging protocol (readPage / saveProduct) is defined and shared by content, background, and popup"
  artifacts:
    - path: "extension/lib/normalizer.ts"
      provides: "parsePrice, parseCount, parseDiscount pure functions (D-09)"
      exports: ["parsePrice", "parseCount", "parseDiscount"]
    - path: "extension/lib/messaging.ts"
      provides: "defineExtensionMessaging ProtocolMap + RawProduct/CapturePayload types"
      exports: ["sendMessage", "onMessage"]
  key_links:
    - from: "extension/lib/messaging.ts"
      to: "@webext-core/messaging"
      via: "defineExtensionMessaging<ProtocolMap>()"
      pattern: "defineExtensionMessaging"
---

<objective>
Build the two foundational extension utilities every downstream entrypoint imports: the D-09 normalization helpers (pure functions) and the typed `@webext-core/messaging` protocol with the `RawProduct`/`CapturePayload` data contract. After this plan, the normalizer tests from 02-01 go GREEN.

Purpose: Normalization (D-09) keeps captured numbers clean for Phase 3 scoring; the messaging protocol is the typed contract between popup↔content↔background that eliminates the MV3 `return true` footgun. These are independent of the backend work in 02-02 (different files) and run in parallel.
Output: extension/lib/normalizer.ts (GREEN tests) + extension/lib/messaging.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-product-capture-shopee/02-RESEARCH.md
@.planning/phases/02-product-capture-shopee/02-PATTERNS.md

<interfaces>
<!-- From PATTERNS.md lib/messaging.ts (lines 337-365) — the contract downstream entrypoints depend on. -->
RawProduct: { name: string|null, price: number|null, discountPct: number,
rating: number|null, reviewCount: number|null, salesCount: number|null,
shop: string|null, productUrl: string, platform: 'shopee'|'lazada'|'tiktok',
fieldsRead: number, fieldsTotal: number }

CapturePayload extends Omit<RawProduct,'fieldsRead'|'fieldsTotal'> { name: string }

ProtocolMap: { readPage(): RawProduct ;
saveProduct(data: CapturePayload): { ok:true; id:string } | { ok:false; error:string } }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement D-09 normalization helpers</name>
  <files>extension/lib/normalizer.ts</files>
  <read_first>
    - .planning/phases/02-product-capture-shopee/02-PATTERNS.md (normalizer.ts section lines 474-510)
    - .planning/phases/02-product-capture-shopee/02-RESEARCH.md (Pattern 6 lines 421-468; Pitfall 7 lines 642-646)
    - extension/lib/normalizer.test.ts (the RED tests from plan 02-01 this must satisfy)
  </read_first>
  <behavior>
    - parsePrice('฿290 - ฿500') === 290 (lowest of a range)
    - parsePrice('฿1,290') === 1290 ; parsePrice('290.00') === 290 ; parsePrice('') === null
    - parseCount('12.3k') === 12300 ; parseCount('1.2พัน') === 1200 ; parseCount('1.5ล้าน') === 1500000 ; parseCount('') === null
    - parseDiscount('10%') === 10 ; parseDiscount('-10%') === 10 ; parseDiscount('') === 0
  </behavior>
  <action>
    Create `extension/lib/normalizer.ts` exporting three PURE functions (no DOM access): `parsePrice(raw: string): number | null` strips `฿`, commas, whitespace, splits on `-`, parses each part, returns `Math.min` of valid numbers or null if none. `parseCount(raw: string): number | null` handles Thai suffixes (ล้าน=1_000_000, แสน=100_000, หมื่น=10_000, พัน=1_000) and English k/m, else plain number; returns rounded integer or null. `parseDiscount(raw: string): number` strips `%` and `-`, returns abs value or 0. Match the implementations in PATTERNS.md lines 482-509 exactly.
  </action>
  <verify>
    <automated>cd extension && bun test lib/normalizer.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `extension/lib/normalizer.ts` exports parsePrice, parseCount, parseDiscount
    - All functions are pure (no `document`/`window`/DOM references)
    - `cd extension && bun test lib/normalizer.test.ts` passes (GREEN) including the Thai abbreviation and price-range cases
  </acceptance_criteria>
  <done>Normalizer GREEN: price-range→lowest, k/Thai abbreviations→integer, currency stripped (D-09).</done>
</task>

<task type="auto">
  <name>Task 2: Define the typed messaging protocol and data contract</name>
  <files>extension/lib/messaging.ts</files>
  <read_first>
    - .planning/phases/02-product-capture-shopee/02-PATTERNS.md (messaging.ts section lines 331-365)
    - .planning/phases/02-product-capture-shopee/02-RESEARCH.md (Pattern 2 lines 248-282)
  </read_first>
  <action>
    Create `extension/lib/messaging.ts` importing `defineExtensionMessaging` from `@webext-core/messaging`. Export the `RawProduct` interface (name|null, price|null, discountPct number, rating|null, reviewCount|null, salesCount|null, shop|null, productUrl string, platform union, fieldsRead number, fieldsTotal number) and `CapturePayload extends Omit<RawProduct,'fieldsRead'|'fieldsTotal'>` with `name: string` (backend requires non-null name). Define `interface ProtocolMap { readPage(): RawProduct; saveProduct(data: CapturePayload): { ok:true; id:string } | { ok:false; error:string } }`. Export `const { sendMessage, onMessage } = defineExtensionMessaging<ProtocolMap>()`. This is type-only plumbing — no runtime behavior to test beyond compilation.
  </action>
  <verify>
    <automated>cd extension && bunx tsc --noEmit 2>&1 | grep -i "messaging" ; test -f lib/messaging.ts && grep -q "defineExtensionMessaging" lib/messaging.ts && grep -q "saveProduct" lib/messaging.ts && echo MESSAGING_OK</automated>
  </verify>
  <acceptance_criteria>
    - `extension/lib/messaging.ts` exports `sendMessage`, `onMessage`, `RawProduct`, `CapturePayload`
    - ProtocolMap declares `readPage` and `saveProduct` with the exact signatures above
    - `bunx tsc --noEmit` reports no type errors originating in messaging.ts
  </acceptance_criteria>
  <done>Typed messaging protocol and RawProduct/CapturePayload contract exported for all entrypoints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw DOM text → normalizer | Untrusted page-controlled strings parsed into numbers (no eval, regex-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-NORM-1 | Tampering | parsePrice/parseCount regex | mitigate | Pure regex/parseFloat parsing; no eval, no dynamic code; malformed input returns null/0 not a crash |
| T-02-NORM-2 | Denial of Service | pathological input strings | accept | Bounded regex over short DOM text fields on a single-operator machine; negligible risk |
</threat_model>

<verification>
- `cd extension && bun test lib/normalizer.test.ts` GREEN
- `bunx tsc --noEmit` clean for the two new files
</verification>

<success_criteria>
- D-09 normalization GREEN across price-range, k/Thai abbreviation, currency-strip cases
- Typed messaging protocol + RawProduct/CapturePayload contract available to downstream entrypoints
</success_criteria>

<output>
Create `.planning/phases/02-product-capture-shopee/02-03-SUMMARY.md` when done
</output>
