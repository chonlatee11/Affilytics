---
phase: 02-product-capture-shopee
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - extension/package.json
  - extension/wxt.config.ts
  - extension/tsconfig.json
  - extension/bunfig.toml
  - extension/lib/normalizer.test.ts
  - extension/lib/parsers.test.ts
  - extension/lib/selectors.test.ts
  - extension/public/selectors.config.json
  - backend/src/routes/products.test.ts
autonomous: false
requirements: [CAP-01, CAP-05]
must_haves:
  truths:
    - "Extension workspace exists with a working `bun test` runner"
    - "Live-verified Shopee CSS selectors are recorded in selectors.config.json (research flag resolved)"
    - "All RED test files exist and fail for the right reason (modules not yet implemented)"
  artifacts:
    - path: "extension/wxt.config.ts"
      provides: "WXT MV3 manifest: host_permissions + web_accessible_resources for selectors.config.json"
      contains: "web_accessible_resources"
    - path: "extension/public/selectors.config.json"
      provides: "Live-verified Shopee selectors (8 fields), editable without rebuild (CAP-05)"
      contains: "shopee"
    - path: "extension/lib/normalizer.test.ts"
      provides: "RED unit tests for D-09 normalization edge cases"
      contains: "parsePrice"
    - path: "backend/src/routes/products.test.ts"
      provides: "RED integration tests for CAP-02/03/04 capture endpoint"
      contains: "/api/products/capture"
  key_links:
    - from: "extension/wxt.config.ts"
      to: "extension/public/selectors.config.json"
      via: "web_accessible_resources declaration"
      pattern: "selectors\\.config\\.json"
---

<objective>
Establish the Phase 2 Wave 0 prerequisites: scaffold the greenfield WXT extension workspace, write the RED test scaffolds for every downstream behavior, and — most importantly — resolve the ROADMAP research flag by verifying the assumed Shopee CSS selectors against a LIVE shopee.co.th product page in Chrome DevTools.

Purpose: Every downstream extension plan depends on this workspace existing and the selectors being real. The selectors in RESEARCH.md/PATTERNS.md are LOW-confidence training-data assumptions; the obfuscated Shopee React class names change per deploy. Shipping unverified selectors guarantees a "0/8 fields captured" failure. This plan converts assumptions into a verified baseline.
Output: A WXT extension workspace, RED test files (backend + extension), and a live-verified `selectors.config.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-product-capture-shopee/02-CONTEXT.md
@.planning/phases/02-product-capture-shopee/02-RESEARCH.md
@.planning/phases/02-product-capture-shopee/02-PATTERNS.md
@.planning/phases/02-product-capture-shopee/02-VALIDATION.md

<interfaces>
<!-- RawProduct contract that the RED parser/normalizer tests assert against. From PATTERNS.md lib/messaging.ts. -->
RawProduct fields: name(string|null), price(number|null), discountPct(number),
rating(number|null), reviewCount(number|null), salesCount(number|null),
shop(string|null), productUrl(string), platform('shopee'|'lazada'|'tiktok'),
fieldsRead(number), fieldsTotal(number=8)

Backend capture contract (from PATTERNS.md products.ts body schema): POST /api/products/capture
accepts { platform, name, price, discountPct?, rating?, reviewCount?, salesCount?, shop?, productUrl }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the WXT extension workspace</name>
  <files>extension/package.json, extension/wxt.config.ts, extension/tsconfig.json, extension/bunfig.toml</files>
  <read_first>
    - .planning/phases/02-product-capture-shopee/02-RESEARCH.md (Pattern 1 lines 217-244 wxt.config; Installation lines 107-115)
    - .planning/phases/02-product-capture-shopee/02-PATTERNS.md (extension/wxt.config.ts section lines 297-327)
    - CLAUDE.md (Technology Stack — WXT 0.20.26, @webext-core/messaging 3.0.2)
  </read_first>
  <action>
    Scaffold a WXT 0.20.26 vanilla-ts extension under `extension/`. Run `cd extension && bun create wxt@latest . --template vanilla-ts` (override pm to bun), then pin `wxt` to 0.20.26 and add `@webext-core/messaging@3.0.2` via `bun add`. Author `wxt.config.ts` with `defineConfig` setting: `manifest.permissions: ['tabs','activeTab']`; `manifest.host_permissions: ['http://localhost/*','http://127.0.0.1/*','*://shopee.co.th/*','*://www.shopee.co.th/*']` (both localhost and 127.0.0.1 because the Phase 1 backend binds 127.0.0.1); `manifest.web_accessible_resources: [{ resources: ['selectors.config.json'], matches: ['*://shopee.co.th/*','*://www.shopee.co.th/*'] }]` — this declaration is mandatory or the content script's runtime fetch of the config is blocked (RESEARCH Pitfall 4). Add a `test` script (`bun test`) to extension/package.json and a `bunfig.toml` so `cd extension && bun test` runs. Do NOT add React.
  </action>
  <verify>
    <automated>cd extension && bun run build 2>&1 | tail -5 && test -f wxt.config.ts && grep -q "web_accessible_resources" wxt.config.ts && echo SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `extension/wxt.config.ts` contains `web_accessible_resources` with `selectors.config.json`
    - `extension/wxt.config.ts` host_permissions include both `http://localhost/*` and `http://127.0.0.1/*`
    - `extension/package.json` pins `wxt@0.20.26` and depends on `@webext-core/messaging@3.0.2`
    - `cd extension && bun run build` exits 0 (WXT manifest generates without error)
    - `cd extension && bun test` is runnable (exits even if no test files yet)
  </acceptance_criteria>
  <done>WXT workspace builds; manifest declares host_permissions + web_accessible_resources; bun test runner wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED test scaffolds (backend capture + extension normalizer/parser/selectors)</name>
  <files>backend/src/routes/products.test.ts, extension/lib/normalizer.test.ts, extension/lib/parsers.test.ts, extension/lib/selectors.test.ts</files>
  <read_first>
    - .planning/phases/02-product-capture-shopee/02-PATTERNS.md (products.test.ts section lines 226-289; normalizer.test.ts lines 514-538)
    - .planning/phases/02-product-capture-shopee/02-RESEARCH.md (Validation Architecture lines 873-899 — Req→Test map)
    - backend/src/routes/settings.test.ts (analog: beforeAll env setup, .handle(new Request()), table cleanup in beforeEach)
  </read_first>
  <behavior>
    backend/src/routes/products.test.ts (imports `productsRoutes` from '../routes/products' — will not exist yet → RED):
    - CAP-02: POST /api/products/capture with a full valid payload returns { ok:true, id } and persists one row
    - CAP-02 upsert: posting the same productUrl twice leaves exactly ONE row, with updated price/rating
    - CAP-03: payload with null rating/reviewCount/salesCount/shop succeeds and stores nulls (no failure)
    - CAP-04: minimal payload (platform + name + price + productUrl only) creates a basic record
    - Validation: payload missing name, or price < 0, or malformed productUrl returns a 4xx (TypeBox reject)
    extension/lib/normalizer.test.ts (imports parsePrice/parseCount/parseDiscount from './normalizer' → RED):
    - parsePrice('฿290 - ฿500') === 290 ; parsePrice('฿1,290') === 1290 ; parsePrice('') === null
    - parseCount('12.3k') === 12300 ; parseCount('1.2พัน') === 1200 ; parseCount('1.5ล้าน') === 1500000
    - parseDiscount('10%') === 10 ; parseDiscount('-10%') === 10 ; parseDiscount('') === 0
    extension/lib/parsers.test.ts (imports parseShopee from '../parsers/shopee' → RED):
    - Given a fixture HTML Document with all selectors present, parseShopee returns RawProduct with fieldsRead===8
    - Given a fixture with only name+price elements, fieldsRead===2 and missing fields are null (CAP-03)
    extension/lib/selectors.test.ts (reads ../public/selectors.config.json → RED until config finalized):
    - JSON.parse succeeds and the `shopee` object has exactly 8 keys (name, price, priceRange, discountBadge, rating, reviewCount, salesCount, shop)
  </behavior>
  <action>
    Author the four test files per the behavior block, modeled on backend/src/routes/settings.test.ts. For backend tests: set `process.env.BUN_ENCRYPTION_KEY = 'a'.repeat(64)` and `process.env.DATABASE_URL = ':memory:'` in `beforeAll`; clear the `products` table in `beforeEach` via dynamic import of db + products; invoke the route in-process with `productsRoutes.handle(new Request('http://localhost/api/products/capture', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(payload) }))`. For parsers.test.ts, build fixture `Document` objects (use `new DOMParser().parseFromString(html, 'text/html')` available in Bun, or a minimal mock matching selectors from selectors.config.json). These tests MUST currently FAIL because the source modules do not exist yet — that is the RED state. Do NOT implement the source modules in this plan.
  </action>
  <verify>
    <automated>cd backend && bun test src/routes/products.test.ts 2>&1 | grep -qiE "fail|cannot find|error" && echo BACKEND_RED_OK; cd ../extension && bun test 2>&1 | grep -qiE "fail|cannot find|error" && echo EXT_RED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/routes/products.test.ts` exists and references `/api/products/capture` and imports `productsRoutes`
    - `extension/lib/normalizer.test.ts` asserts parsePrice('฿290 - ฿500')===290 and parseCount('1.2พัน')===1200
    - `extension/lib/parsers.test.ts` asserts a fixture-driven RawProduct with fieldsRead count
    - All four test files FAIL on run (RED) because source modules are not yet implemented
  </acceptance_criteria>
  <done>Four RED test files exist; each fails because its target module is unimplemented (correct RED reason, not a syntax error).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Verify live Shopee selectors against a real product page (RESEARCH FLAG)</name>
  <files>extension/public/selectors.config.json</files>
  <read_first>
    - .planning/phases/02-product-capture-shopee/02-RESEARCH.md (Shopee Selector Research lines 650-687; Pattern 5 lines 388-409; inspection procedure lines 411-418)
    - .planning/phases/02-product-capture-shopee/02-PATTERNS.md (selectors.config.json section lines 579-598)
  </read_first>
  <what-built>
    Claude has written `extension/public/selectors.config.json` pre-filled with the ASSUMED selector candidates from RESEARCH.md Pattern 5 (8 fields under a `shopee` key, each value a comma-separated selector list with data-sqe + obfuscated-class fallbacks). These values are LOW-confidence training data and almost certainly partially wrong for the current shopee.co.th build.
  </what-built>
  <how-to-verify>
    1. Open any live product page on https://shopee.co.th in Chrome (e.g. search a product, open its detail page).
    2. Open DevTools → Elements. For each of the 8 fields (product name, price, price-range, discount badge, rating, review count, sales count, shop name), find the element holding that value.
    3. In the DevTools Console, test the candidate selector: `document.querySelector("<selector from config>")?.textContent`. If it returns the right text, keep it; if null, right-click the element → Copy → Copy selector, and replace the value in `extension/public/selectors.config.json`. Prefer stable `data-sqe="..."` attributes over obfuscated `_3Oj5_n`-style classes; keep an obfuscated-class fallback after a comma.
    4. Confirm `JSON.parse` still works (valid JSON, exactly the 8 keys: name, price, priceRange, discountBadge, rating, reviewCount, salesCount, shop).
    5. Report which selectors you changed so Claude can run `cd extension && bun test lib/selectors.test.ts` to confirm the config parses with 8 keys.
  </how-to-verify>
  <verify>
    <automated>cd extension && bun test lib/selectors.test.ts 2>&1 | tail -5</automated>
    <human-check>Each of the 8 selectors returns the expected value via document.querySelector on a live shopee.co.th product page</human-check>
  </verify>
  <acceptance_criteria>
    - `extension/public/selectors.config.json` is valid JSON with a `shopee` object containing exactly 8 keys
    - Operator has confirmed each selector resolves to the correct element on a live Shopee page (or recorded which remain unverifiable and why)
    - `cd extension && bun test lib/selectors.test.ts` passes (config parses, 8 keys present)
  </acceptance_criteria>
  <resume-signal>Type "approved" with the list of any selectors you changed, or describe which fields could not be located.</resume-signal>
  <done>selectors.config.json holds live-verified Shopee selectors; the research flag is resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shopee page DOM → content script | Untrusted page markup; selectors read text the page controls |
| Extension build artifacts | web_accessible_resources exposes selectors.config.json to matched origins only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-W0-1 | Information Disclosure | web_accessible_resources | mitigate | Scope `matches` to `*://shopee.co.th/*` only — config not exposed to arbitrary origins |
| T-02-W0-2 | Tampering | selectors.config.json | accept | Operator-edited local file on a single-operator machine; no remote write path |
| T-02-SC | Tampering | bun add wxt / @webext-core/messaging | mitigate | RESEARCH Package Legitimacy Audit cleared both as Approved (no postinstall); pinned versions |
</threat_model>

<verification>
- `cd extension && bun run build` exits 0
- `cd extension && bun test lib/selectors.test.ts` passes (config valid, 8 keys)
- Backend + extension RED tests fail for the correct reason (unimplemented modules)
- Operator has signed off on live-selector verification
</verification>

<success_criteria>
- WXT workspace exists, builds, and runs `bun test`
- selectors.config.json holds live-verified selectors (research flag resolved)
- RED test scaffolds exist for CAP-02/03/04 (backend) and CAP-01/D-09 (extension)
</success_criteria>

<output>
Create `.planning/phases/02-product-capture-shopee/02-01-SUMMARY.md` when done
</output>
