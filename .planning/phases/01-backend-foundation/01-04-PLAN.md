---
phase: 01-backend-foundation
plan: 04
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/package.json
  - backend/.gitignore
  - backend/src/db/client.ts
  - backend/index.ts
gap_closure: true
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03]
must_haves:
  truths:
    - "`bun run dev` boots the backend (entry point index.ts at root, not src/) and GET /health returns 200"
    - "The in-memory/test DB path no longer writes `file::memory:?cache=shared{,-shm,-wal}` files to disk; no such files are produced by `bun test`"
    - "`file::memory:*` is gitignored AND the already-leaked files at backend/ and project root are removed from the working tree"
    - "index.ts wraps `.listen(3000)` in `if (import.meta.main)` so importing index.ts in a test does NOT bind port 3000; the misleading line-67 comment is corrected"
    - "WAL/PRAGMA DB tests still pass after the client.ts in-memory fix; full `bun test` suite stays green"
  artifacts:
    - path: "backend/package.json"
      provides: "dev script pointing at the real entry point"
      contains: "bun run --watch index.ts"
    - path: "backend/.gitignore"
      provides: "ignore pattern for leaked in-memory test DB files"
      contains: "file::memory:*"
    - path: "backend/src/db/client.ts"
      provides: "openDatabase() in-memory path that does not write file::memory files to disk"
      contains: "isMemory"
    - path: "backend/index.ts"
      provides: "import.meta.main-guarded listen; single migrate at boot"
      contains: "import.meta.main"
  key_links:
    - from: "backend/package.json"
      to: "backend/index.ts"
      via: "dev script runs the root entry point"
      pattern: "--watch index.ts"
    - from: "backend/index.ts"
      to: "backend/src/db/client.ts"
      via: "single migrate path at boot (collapse WR-01 double-migrate)"
      pattern: "import.meta.main"
---

<objective>
Close the three mechanical Phase 1 verification blockers without touching the verified-working crypto, schema, settings, or health code paths beyond what each fix strictly requires:

1. Gap 1 (SC-1 partial, BLOCKER): `package.json` `dev` script points at the non-existent `src/index.ts`; the real entry point is `backend/index.ts` (root). `bun run dev` fails. Fix the script string.
2. Gap 2 (BLOCKER): `client.ts` maps `':memory:'` to `'file::memory:?cache=shared'`, which bun:sqlite writes to disk as a real file, leaking 3 files in backend/ and 1 at project root — none gitignored. Fix the in-memory path so no such files are written, add `file::memory:*` to `.gitignore` as defense-in-depth, and remove the already-leaked files from the working tree.
3. Gap 3 (BLOCKER + WR-01): `index.ts` calls `.listen(3000)` unconditionally under a comment that falsely claims a guard exists; wrap it in `if (import.meta.main)`. Also collapse the double `migrate()` call (once in `openDatabase()`, once in index.ts) to a single explicit migrate path.

Purpose: restore the published developer run workflow, stop test-DB files from being committable, and make index.ts safely importable by tests.
Output: corrected package.json dev script, fixed client.ts in-memory handling, updated .gitignore + removed leaked files, guarded + de-duplicated boot in index.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-backend-foundation/01-VERIFICATION.md
@.planning/phases/01-backend-foundation/01-REVIEW.md
@.planning/phases/01-backend-foundation/01-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Verified-working from Plan 02/03 — do NOT change their signatures. -->
backend/src/db/client.ts → openDatabase(path?): AppDatabase ; db (singleton) ; rawSqlite(db): Database
backend/env.ts           → requireEncryptionKey(): Buffer
backend/index.ts         → app (exported Elysia, bound 127.0.0.1:3000) — server.test.ts imports `app` and calls app.stop() in afterAll

<!-- Existing in-memory path (the bug): -->
openDatabase(':memory:') currently sets resolvedPath = 'file::memory:?cache=shared' and passes it to new Database(), creating real on-disk files.
client.ts already runs `migrate(db, ...)` inside openDatabase() (line ~67), AND index.ts runs migrate(db,...) again at line ~32 → WR-01 double migrate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dev script + guard listen + collapse double-migrate in index.ts</name>
  <files>backend/package.json, backend/index.ts</files>
  <read_first>
    - backend/package.json (the dev script string `bun run --watch src/index.ts`)
    - backend/index.ts (lines ~30-32 the explicit migrate call; lines ~66-72 the comment + unconditional .listen(3000) + the console.log)
    - backend/src/db/client.ts (confirm openDatabase() already calls migrate() at boot — this is why index.ts's migrate is redundant per WR-01)
  </read_first>
  <action>
    backend/package.json — change the `dev` script from `bun run --watch src/index.ts` to `bun run --watch index.ts` (the entry point is index.ts at the backend/ root). Leave the other scripts (setup, migrate:generate, migrate:run, test) unchanged. NOTE: `migrate:run` still points at `src/db/migrate.ts` — leave it as-is, it is a separate standalone script and out of scope for this gap.
    backend/index.ts — (a) Remove the unconditional `.listen(3000)` from the Elysia chain and instead, after `export const app = ...` is fully constructed (chain ends at `.use(settingsRoutes)`), add a guarded block: `if (import.meta.main) { app.listen(3000); console.log(\`Listening on http://${app.server?.hostname}:${app.server?.port}\`) }`. Keep `export const app` intact so server.test.ts can still import it. (b) Replace the misleading comment block on lines ~66-67 (the one claiming "we guard with Bun.main check") with an accurate comment stating the listen is guarded by `import.meta.main` so importing the module in tests does not bind the port. (c) WR-01: remove the redundant explicit `migrate()` at lines ~30-32 (`import { migrate } from 'drizzle-orm/bun-sqlite/migrator'` and the `migrate(db, { migrationsFolder: './drizzle' })` call), since `openDatabase()` in client.ts already applies migrations at boot. Add a one-line comment at the former call site noting migrations are applied inside openDatabase() (single source of truth). Do NOT alter the requireEncryptionKey → db import → seedDefaultSettings → Elysia ordering, the 127.0.0.1 hostname, or the CORS allowlist.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; grep -q '"dev": "bun run --watch index.ts"' package.json &amp;&amp; grep -q 'import.meta.main' index.ts &amp;&amp; test "$(grep -c 'migrate(db' index.ts)" = "0" &amp;&amp; BUN_ENCRYPTION_KEY=$(printf 'b%.0s' {1..64}) bun test src/server.test.ts 2>&amp;1 | tail -3 &amp;&amp; echo TASK1_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"dev": "bun run --watch index.ts"' backend/package.json` >= 1 (dev script fixed).
    - `grep -c 'import.meta.main' backend/index.ts` >= 1 (listen is guarded).
    - `grep -c 'migrate(db' backend/index.ts` == 0 (WR-01 double-migrate collapsed; openDatabase() is now the single migrate path).
    - The line-67 comment no longer claims a `Bun.main` guard exists; it accurately describes the `import.meta.main` guard.
    - `bun test src/server.test.ts` GREEN (app still exported, binds 127.0.0.1 when listened, stops cleanly).
    - `export const app` remains present in index.ts.
  </acceptance_criteria>
  <done>`bun run dev` resolves to the real entry point; index.ts no longer binds port 3000 on import and no longer double-applies migrations; the misleading comment is corrected. server.test.ts still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Stop in-memory DB disk leakage; gitignore + remove leaked files</name>
  <files>backend/src/db/client.ts, backend/.gitignore</files>
  <read_first>
    - backend/src/db/client.ts (lines ~39-50: the `isMemory` branch mapping ':memory:' → 'file::memory:?cache=shared', the mkdirSync guard, and the new Database(resolvedPath) call; lines ~52-55 the WAL + busy_timeout PRAGMAs)
    - backend/.gitignore (current patterns — *.db, *.db-wal, *.db-shm, data/ ; note `file::memory:*` is missing)
    - backend/src/db/schema.test.ts, backend/src/db/pragma.test.ts (the WAL/PRAGMA tests that must still pass — confirm what they assert about journal_mode before changing the in-memory path)
  </read_first>
  <action>
    backend/src/db/client.ts — replace the `file::memory:?cache=shared` mapping in `openDatabase()` so the in-memory/test path no longer writes that string to disk. Implement per CR-01's recommended fix: when `dbPath === ':memory:'`, resolve to a unique per-process temp file under the OS temp dir using `tmpdir()` from `node:os` and `randomUUID()` from `node:crypto` (e.g. `<tmpdir>/affilytics-test-<uuid>.db`) instead of the `file::memory:?cache=shared` literal — this preserves WAL support (WAL needs a real on-disk file, which is why a true `:memory:` handle was avoided) while keeping the file OUT of the repo tree. Keep the existing PRAGMA journal_mode=WAL and busy_timeout=5000 calls and the migrate() call unchanged so the WAL/PRAGMA tests still assert `journal_mode === 'wal'`. Register best-effort cleanup so the temp file (+ `-wal`/`-shm` siblings) is removed when the raw handle is closed: wrap the bun:sqlite handle's `close` to also `unlinkSync` the temp paths inside a try/catch (ignore unlink errors). Skip the existing `mkdirSync(dirname(...))` directory-creation for the memory path (tmpdir already exists). Do NOT change the non-memory (file-based production) branch behavior, the rawHandles WeakMap, rawSqlite(), or the exported `db` singleton.
    backend/.gitignore — add a `file::memory:*` pattern (defense-in-depth in case any future regression re-introduces the literal filename) under the SQLite section, with a short comment. Keep all existing patterns.
    Removal of already-leaked files: delete the four leaked artifacts from the working tree — `backend/file::memory:?cache=shared`, `backend/file::memory:?cache=shared-shm`, `backend/file::memory:?cache=shared-wal`, and the project-root `file::memory:?cache=shared`. These are untracked, so `rm -f` (quote the literal filenames) removes them; do not `git rm` (they were never committed).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; grep -c 'file::memory' .gitignore &amp;&amp; grep -q 'tmpdir\|randomUUID' src/db/client.ts &amp;&amp; BUN_ENCRYPTION_KEY=$(printf 'c%.0s' {1..64}) bun test src/db/ 2>&amp;1 | tail -5 &amp;&amp; ls 'file::memory:?cache=shared' 2>/dev/null &amp;&amp; echo LEAK_STILL_PRESENT || echo NO_LEAK_IN_BACKEND</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'file::memory' backend/.gitignore` >= 1 (ignore pattern present).
    - After `bun test src/db/`, no file matching `backend/file::memory:*` exists (`ls backend/'file::memory:?cache=shared' 2>/dev/null` returns nothing).
    - The four previously-leaked files (3 in backend/, 1 at project root) are gone from the working tree (`git status --porcelain | grep -c 'file::memory'` == 0).
    - `bun test src/db/` GREEN — WAL/PRAGMA tests still assert journal_mode 'wal' and pass with the temp-file in-memory path.
    - client.ts in-memory branch uses `tmpdir()`/`randomUUID()` and no longer contains the `file::memory:?cache=shared` literal (`grep -c 'file::memory' backend/src/db/client.ts` == 0).
  </acceptance_criteria>
  <done>The test DB path writes only to the OS temp dir (cleaned up on close), `file::memory:*` is gitignored as a backstop, and the four leaked files are removed. DB/WAL tests remain green.</done>
</task>

</tasks>

<verification>
- `bun run dev` boots; `curl http://127.0.0.1:3000/health` returns 200 (documented manual check).
- `grep import.meta.main backend/index.ts` matches; importing index.ts in a test does not bind port 3000.
- `grep -c 'migrate(db' backend/index.ts` == 0 (single migrate path via openDatabase()).
- `bun test src/db/` GREEN; no `file::memory:*` files produced; `.gitignore` ignores the pattern; leaked files removed.
- Full `bun test` suite stays green.
</verification>

<success_criteria>
- FOUND-01: published `bun run dev` workflow boots the localhost backend and serves GET /health 200.
- FOUND-02: WAL + busy_timeout DB tests still pass after the in-memory path fix.
- FOUND-03: 127.0.0.1 binding and CORS allowlist unchanged; import.meta.main guard prevents accidental port binds.
- Repo hygiene: no committable test-DB files; misleading comment removed; WR-01 double-migrate resolved.
</success_criteria>

<output>
Create `.planning/phases/01-backend-foundation/01-04-SUMMARY.md` when done.
</output>
