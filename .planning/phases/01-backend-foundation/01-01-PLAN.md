---
phase: 01-backend-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/package.json
  - backend/tsconfig.json
  - backend/bunfig.toml
  - backend/drizzle.config.ts
  - backend/.gitignore
  - backend/.env.example
  - backend/src/db/schema.test.ts
  - backend/src/db/pragma.test.ts
  - backend/src/services/cryptoService.test.ts
  - backend/src/routes/health.test.ts
  - backend/src/routes/settings.test.ts
  - backend/src/seed.test.ts
  - backend/src/index.test.ts
  - backend/src/server.test.ts
  - backend/test/helpers/fbMock.ts
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03, SET-01]
must_haves:
  truths:
    - "A Bun project exists in backend/ with all Phase 1 dependencies installed (no Phase 5+ packages)"
    - "All 8 Wave 0 test files from VALIDATION.md exist and fail RED (modules under test do not exist yet)"
    - "bun test discovers and runs the test files (compilation may fail on missing imports — that is the expected RED state)"
    - "FB Graph API is mocked via bun:test mock — no live network calls in any test"
    - "SQLite DB file and .env are gitignored"
  artifacts:
    - path: "backend/package.json"
      provides: "Bun project manifest with scripts (setup, dev, migrate:generate, migrate:run, test) and Phase 1 deps"
      contains: "drizzle-orm"
    - path: "backend/drizzle.config.ts"
      provides: "drizzle-kit config: dialect sqlite, schema path, out ./drizzle"
      contains: "dialect: 'sqlite'"
    - path: "backend/src/db/schema.test.ts"
      provides: "WAL + 7-table existence test (FOUND-02)"
      contains: "journal_mode"
    - path: "backend/src/services/cryptoService.test.ts"
      provides: "AES-256-GCM round-trip + wrong-key null test (SET-01)"
      contains: "decrypt"
    - path: "backend/test/helpers/fbMock.ts"
      provides: "Reusable bun:test fetch mock for graph.facebook.com"
      contains: "graph.facebook.com"
    - path: "backend/.gitignore"
      provides: "Ignores .env, data/*.db, node_modules"
      contains: ".env"
  key_links:
    - from: "backend/src/db/schema.test.ts"
      to: "backend/src/db/client.ts (Plan 02)"
      via: "import of db / openDatabase"
      pattern: "from '.*db/client'"
    - from: "backend/src/routes/settings.test.ts"
      to: "backend/test/helpers/fbMock.ts"
      via: "import of FB fetch mock"
      pattern: "fbMock"
---

<objective>
Stand up the greenfield `backend/` Bun project skeleton and author ALL Wave 0 test files defined in VALIDATION.md. This is the Walking Skeleton's test-and-scaffold foundation: the test suite encodes the binding Requirement→Test contract before any implementation exists, so later waves implement against RED tests.

Purpose: Establish the Nyquist sampling harness (every FOUND/SET requirement has an automated verify) and the project manifest so Waves 1–2 can implement against a known contract. No test infrastructure exists yet — this is greenfield.

Output: `backend/package.json`, tsconfig, bunfig, drizzle.config, .gitignore, .env.example, and all 8 VALIDATION.md Wave 0 test files plus a shared FB fetch-mock helper. Tests intentionally fail RED (their target modules land in Waves 1–2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-backend-foundation/01-CONTEXT.md
@.planning/phases/01-backend-foundation/01-RESEARCH.md
@.planning/phases/01-backend-foundation/01-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Contracts that the test files assert against. Implemented in Waves 1-2. Executor uses these names directly. -->

Module surface the tests import (do NOT implement here — only reference in test imports):
- backend/src/db/client.ts        → exports `db` (drizzle instance), `openDatabase(path?)`, `rawSqlite` (the bun:sqlite Database)
- backend/src/db/schema.ts        → exports tables: products, productExtras, postDrafts, publishedPosts, resultEntries, pages, settings
- backend/src/services/cryptoService.ts → exports `encrypt(plaintext: string): string`, `decrypt(stored: string): string | null`
- backend/src/services/fbService.ts → exports `verifyToken(token: string, expectedPageId: string): Promise<{ ok: boolean; pageName?: string; pageId?: string; dataAccessExpiresAt?: number; error?: string }>`
- backend/src/routes/health.ts    → exports `healthRoutes` (Elysia instance)
- backend/src/routes/settings.ts  → exports `settingsRoutes` (Elysia instance)
- backend/src/seed.ts             → exports `seedDefaultSettings(): void | Promise<void>`
- backend/src/env.ts              → exports `requireEncryptionKey(): Buffer` (throws/exits if BUN_ENCRYPTION_KEY missing)
- backend/src/index.ts            → app entry; exports `app` (Elysia) for in-process .handle() tests

Storage format for crypto: `base64(iv):base64(authTag):base64(ciphertext)` (colon-joined triple).
FB verify endpoint: `GET https://graph.facebook.com/v22.0/me?fields=id,name&access_token=<token>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the backend Bun project + config files</name>
  <files>backend/package.json, backend/tsconfig.json, backend/bunfig.toml, backend/drizzle.config.ts, backend/.gitignore, backend/.env.example</files>
  <read_first>
    - .planning/phases/01-backend-foundation/01-RESEARCH.md (§ Installation, § Pattern 8 package.json scripts, § Pattern 9 drizzle.config.ts, § What NOT to Install)
    - CLAUDE.md (§ Technology Stack — locked versions; § Stack Patterns by Layer)
  </read_first>
  <action>
    Create the backend/ project. Run `bun init -y` semantics manually via the Write tool (do NOT run an interactive init). Author package.json with: name "affilytics-backend", type "module", and scripts — `setup` ("bun scripts/setup.ts"), `dev` ("bun run --watch src/index.ts"), `migrate:generate` ("drizzle-kit generate --config drizzle.config.ts"), `migrate:run` ("bun run src/db/migrate.ts"), `test` ("bun test"). Dependencies at locked versions: elysia@1.4.29, @elysiajs/cors@1.4.2, @elysiajs/swagger@1.3.1, drizzle-orm@0.45.2. devDependencies: drizzle-kit@0.31.10, typescript@6.0.3, @types/bun (latest). Do NOT add croner, ollama, zod, dotenv, better-sqlite3, or facebook-nodejs-business-sdk (Phase 5+/forbidden per RESEARCH § What NOT to Install). Then run `cd backend && bun install` to materialize node_modules and bun.lock.
    Author tsconfig.json targeting ESNext modules with "types": ["bun"], strict true, moduleResolution "bundler". Author bunfig.toml with a [test] section if needed (default discovery is fine; keep minimal). Author drizzle.config.ts via defineConfig from drizzle-kit: dialect 'sqlite', schema './src/db/schema.ts', out './drizzle', dbCredentials.url = process.env.DATABASE_URL ?? 'data/affilytics.db'. Author .gitignore including: node_modules, .env, data/, *.db, *.db-wal, *.db-shm. Author .env.example with a commented BUN_ENCRYPTION_KEY= placeholder and DATABASE_URL=data/affilytics.db.
  </action>
  <verify>
    <automated>cd backend && bun install >/dev/null 2>&1 && test -d node_modules/drizzle-orm && test -d node_modules/elysia && grep -q "drizzle-orm" package.json && grep -qE "croner|ollama|zod|dotenv|better-sqlite3" package.json && echo FORBIDDEN_DEP_PRESENT || echo DEPS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && bun install` exits 0 and creates node_modules/drizzle-orm and node_modules/elysia.
    - package.json scripts include setup, dev, migrate:generate, migrate:run, test (exact keys).
    - `grep -E "croner|ollama|zod|dotenv|better-sqlite3" backend/package.json` returns no match (forbidden deps absent).
    - drizzle.config.ts contains the literal `dialect: 'sqlite'` and `out: './drizzle'`.
    - .gitignore contains the lines `.env` and `data/` (DB file + key never committed — threat T-1-KEY).
  </acceptance_criteria>
  <done>backend/ is an installable Bun project at locked Phase 1 versions with no forbidden deps; config files for TS, Bun, Drizzle present; secrets gitignored.</done>
</task>

<task type="auto">
  <name>Task 2: Author DB + crypto + startup Wave 0 test files (db, pragma, cryptoService, index, server)</name>
  <files>backend/src/db/schema.test.ts, backend/src/db/pragma.test.ts, backend/src/services/cryptoService.test.ts, backend/src/index.test.ts, backend/src/server.test.ts, backend/test/helpers/fbMock.ts</files>
  <read_first>
    - .planning/phases/01-backend-foundation/01-VALIDATION.md (§ Per-Task Verification Map, § Wave 0 Requirements — exact file names + asserted behaviors)
    - .planning/phases/01-backend-foundation/01-RESEARCH.md (§ Pattern 1 PRAGMAs, § Pattern 4 crypto, § Entity Schema Reference — 7 table names, § Pitfall 1 busy_timeout, § Pitfall 6 decrypt null)
    - The <interfaces> block in this plan's <context> (import surface)
  </read_first>
  <action>
    Author the Wave-1-target test files (they will fail RED until Plan 02 lands). All tests use `import { test, expect, describe, mock, beforeEach } from 'bun:test'`.
    backend/src/db/schema.test.ts — import `openDatabase` from '../db/client'; open an in-memory or temp-file DB, run `migrate`, then query `sqlite_master` (via the raw bun:sqlite handle) for table names; assert exactly these 7 tables exist: products, product_extras, post_drafts, published_posts, result_entries, pages, settings. Also assert `PRAGMA journal_mode` returns 'wal' (FOUND-02, threat T-1-LAN context).
    backend/src/db/pragma.test.ts — open the DB via the project client; assert `PRAGMA busy_timeout` returns 5000 on the live connection (FOUND-02). Note in a comment that busy_timeout is per-connection (Pitfall 1).
    backend/src/services/cryptoService.test.ts — set BUN_ENCRYPTION_KEY to a fixed 64-hex test key in beforeEach; assert encrypt→decrypt round-trips a sample token (SET-01, T-1-CRYPTO); assert the stored ciphertext string does NOT contain the plaintext substring; assert decrypt() of a value encrypted under a DIFFERENT key returns null, not throws (SET-01, T-1-DISCONNECT, Pitfall 6).
    backend/src/index.test.ts — assert that calling `requireEncryptionKey()` from '../env' (or importing src/index startup) with BUN_ENCRYPTION_KEY unset throws or exits non-zero (D-03, T-1-KEY). Use a child-process spawn (`Bun.spawn`) running the entry with the env var deleted, asserting non-zero exit, OR call requireEncryptionKey() in a try/catch asserting it throws — prefer the requireEncryptionKey() unit form.
    backend/src/server.test.ts — assert the server/app binds to hostname '127.0.0.1' (FOUND-03, T-1-LAN). Import `app` from '../index' and assert `app.server?.hostname === '127.0.0.1'` after listen, or assert the serve config object literal is '127.0.0.1'.
    backend/test/helpers/fbMock.ts — export a helper that installs a `mock` over global fetch matching `graph.facebook.com`, returning either a success body `{ id, name }` or an error body `{ error: { message } }` based on a passed fixture; export `installFbMock(fixture)` and `restoreFetch()`. No live network.
  </action>
  <verify>
    <automated>cd backend && bun test src/db/ src/services/cryptoService.test.ts src/index.test.ts src/server.test.ts 2>&1 | grep -qiE "fail|error|cannot find|0 pass" && echo RED_AS_EXPECTED || echo UNEXPECTED_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - All five test files and test/helpers/fbMock.ts exist at the paths in VALIDATION.md.
    - `grep -c "journal_mode" backend/src/db/schema.test.ts` ≥ 1 and the file lists all 7 table names (products, product_extras, post_drafts, published_posts, result_entries, pages, settings).
    - cryptoService.test.ts asserts decrypt-with-wrong-key returns null (string `toBeNull` or `=== null` present).
    - pragma.test.ts asserts busy_timeout === 5000.
    - fbMock.ts references `graph.facebook.com` and uses `mock` from bun:test (no real fetch).
    - `bun test` over these files is RED (failing imports / assertions) — proving the contract precedes implementation.
  </acceptance_criteria>
  <done>DB, crypto, env-fail-fast, and server-binding behaviors are encoded as failing tests with a reusable FB fetch mock; suite runs and reports RED.</done>
</task>

<task type="auto">
  <name>Task 3: Author route + seed Wave 0 test files (health, settings, seed)</name>
  <files>backend/src/routes/health.test.ts, backend/src/routes/settings.test.ts, backend/src/seed.test.ts</files>
  <read_first>
    - .planning/phases/01-backend-foundation/01-VALIDATION.md (§ Per-Task Verification Map rows: health, settings ×3, seed)
    - .planning/phases/01-backend-foundation/01-RESEARCH.md (§ Code Examples — Health Route, Settings GET, Default Settings Seed; § Pattern 6 settings POST; § Pitfall 3 token-in-response; § Pattern 7 FB verify)
    - backend/test/helpers/fbMock.ts (created in Task 2)
    - The <interfaces> block in this plan's <context>
  </read_first>
  <action>
    Author the Wave-2-target route/seed test files (fail RED until Plan 03). Use Elysia's in-process `.handle(new Request(...))` to exercise routes without binding a socket; mock FB via test/helpers/fbMock.ts.
    backend/src/routes/health.test.ts — import `healthRoutes`; `GET /health` via `.handle()`; assert status 200 and JSON body has `status: 'ok'` and a db-connected indicator (FOUND-01, D-05).
    backend/src/routes/settings.test.ts — three cases. (a) T-1-EXPOSE: after a (mocked) successful fb-connect, `GET /api/settings` response JSON must NOT contain `accessTokenEnc` nor the raw token plaintext anywhere (assert the serialized response string excludes the token). (b) T-1-FBVERIFY: `POST /api/settings/fb-connect` with the FB mock returning an error body responds with a 4xx (422) and does NOT persist a token. (c) T-1-DISCONNECT: when a pages row holds an undecryptable token, `GET /api/settings` returns `fbConnected: false` and the handler does NOT throw (backend stays up — D-04). Use installFbMock for (a) and (b).
    backend/src/seed.test.ts — import `seedDefaultSettings`; call it twice against a fresh migrated DB; assert exactly one settings row with id 'default' exists after the second call (idempotent — D-08); assert default draftMode 'template', defaultTone 'casual', dailyPostLimit present, and scoreWeights parses to an object with the 5 weight keys.
  </action>
  <verify>
    <automated>cd backend && bun test src/routes/ src/seed.test.ts 2>&1 | grep -qiE "fail|error|cannot find|0 pass" && echo RED_AS_EXPECTED || echo UNEXPECTED_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - health.test.ts asserts HTTP 200 and `status: 'ok'` from `GET /health`.
    - settings.test.ts contains all three threat assertions: token-never-in-response (T-1-EXPOSE), invalid-token-rejected (T-1-FBVERIFY), undecryptable-token-stays-up (T-1-DISCONNECT). `grep -c "accessTokenEnc" backend/src/routes/settings.test.ts` ≥ 1 (asserting its ABSENCE in responses).
    - seed.test.ts calls `seedDefaultSettings()` twice and asserts a single 'default' row (idempotency).
    - All three files import from test/helpers/fbMock.ts where FB calls occur (no live network).
    - `bun test src/routes/ src/seed.test.ts` reports RED.
  </acceptance_criteria>
  <done>Health, settings (3 security behaviors), and idempotent-seed behaviors are encoded as failing tests using the in-process Elysia handler and FB mock; suite reports RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator filesystem → repo (git) | Secrets (.env key, SQLite DB) must never cross into version control |
| test harness → external (Meta Graph API) | Tests must never make live calls to graph.facebook.com |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-KEY | Information Disclosure | `.env` BUN_ENCRYPTION_KEY in git | mitigate | `.gitignore` includes `.env`; `.env.example` carries only a placeholder (Task 1) |
| T-1-EXPOSE | Information Disclosure | settings GET response | mitigate | settings.test.ts asserts token never serialized in any response (Task 3) — drives Plan 03 explicit column selection |
| T-1-FBVERIFY | Spoofing | pasted FB token | mitigate | settings.test.ts asserts invalid token rejected before persistence (Task 3) |
| T-1-DISCONNECT | Denial of Service | undecryptable stored token | mitigate | settings.test.ts asserts backend stays up + disconnected state (Task 3) |
| T-1-CRYPTO | Tampering | AES-GCM auth tag | mitigate | cryptoService.test.ts asserts round-trip + wrong-key null (Task 2) |
| T-1-SC | Tampering | bun add of npm packages | mitigate | All Phase 1 packages pre-approved in RESEARCH § Package Legitimacy Audit (npm, high downloads, no postinstall); no [ASSUMED]/[SUS] packages — no install checkpoint required |
</threat_model>

<verification>
- `cd backend && bun install` succeeds; node_modules present.
- All 8 VALIDATION.md Wave 0 test files exist at their exact paths plus test/helpers/fbMock.ts.
- `bun test` runs and reports RED (no target modules yet) — the expected pre-implementation state.
- No forbidden dependencies in package.json.
- `.env` and `data/` gitignored.
</verification>

<success_criteria>
- backend/ Bun project installs cleanly at locked Phase 1 versions.
- Every FOUND-01/02/03 and SET-01 behavior in VALIDATION.md has a corresponding failing test file.
- FB Graph API is mocked; zero live network in tests.
- Secrets are gitignored.
</success_criteria>

<output>
Create `.planning/phases/01-backend-foundation/01-01-SUMMARY.md` when done.
</output>
